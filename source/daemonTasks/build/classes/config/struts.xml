<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>
	
	<!-- 将Action的创建交给spring来管理 -->  
    <constant name="struts.objectFactory" value="spring" />  
	
	<!-- 更改struts2请求Action的后缀名，默认为action。若想去掉后缀，设为","即可 -->
	<constant name="struts.action.extension" value=","></constant>

		<package name="defaultActions" namespace="/" extends="struts-default">
		<action name="addTask" class="addTaskAction" method="addTask">
			<result name="success">/getTaskResult.jsp</result>
		</action>
	</package>
	
</struts>